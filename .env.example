# ── Required ──────────────────────────────────────────────────────────────────
BOT_TOKEN=your_bot_token_here

# ── Sponsor channels ──────────────────────────────────────────────────────────
# Comma-separated list of channels users MUST join before using the bot.
# Use @username for public channels, or -100xxxxxxxxxx for private channels.
# Examples:
#   SPONSOR_CHANNELS=@mychannel
#   SPONSOR_CHANNELS=@channel1,@channel2,-1001234567890
SPONSOR_CHANNELS=@your_channel_username

# Legacy single-channel support (still works if SPONSOR_CHANNELS is not set)
# CHANNEL_USERNAME=@your_channel_username
# CHANNEL_ID=-1001234567890

# ── Admin ─────────────────────────────────────────────────────────────────────
# Telegram user ID of the admin.
# The admin can run: /broadcast, /add_channel, /remove_channel, /list_channels, /send_csv
ADMIN_USER_ID=123456789

# ── Report channel ────────────────────────────────────────────────────────────
# Channel ID or username where new-user notifications + CSV updates are sent.
# The bot must be an admin of this channel.
REPORT_CHANNEL_ID=-1001234567890
